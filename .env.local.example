# Copy to .env.local and fill in your values

# Backend URL — used by Next.js rewrite to proxy /api/* to the backend
# Local dev: point to your local FastAPI server
BACKEND_URL=http://localhost:8000

# Google OAuth — Client ID from Google Cloud Console (safe to expose publicly)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=xxxxxxxxxxxx.apps.googleusercontent.com
